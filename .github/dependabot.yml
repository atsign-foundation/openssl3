version: 2
updates:
  # Maintain dependencies for GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
    cooldown:
      default-days: 7
  # Dart workspace (packages/openssl3, tool, example/cli resolve from the root)
  - package-ecosystem: "pub"
    directories:
    - "/"
    - "/example/flutter_app"
    schedule:
      interval: "daily"
    cooldown:
      default-days: 7
