/// Prebuilt OpenSSL 3.5 LTS `libcrypto` for Dart and Flutter, delivered as a
/// code asset by `hook/build.dart`.
///
/// This is the package skeleton. Bindings, capabilities and the hook land in
/// later commits; see `PLAN.md` at the repository root.
library;
